# Config file for an example Majora's Mask: Recompiled mod.

# Fields that end up in the mod's manifest.
[manifest]

# Unique ID of this mod. Pick something long enough that it'll never be the same as any other mod.
# The name displayed in the mod management menu is separate from this so this doesn't need to be human readable.
id = "mnsg_multiplayer_anchor"

# Version of this mod.
version = "0.1.0"

# The name that will show up for this mod in the mod menu. This should be human readable.
display_name = "Anchor Multiplayer"

# The description that will show up when this mod is displayed in the mod menu. This should be human readable.
description = """
Adds Anchor co-op support to Mystical Ninja Starring Goemon: Recompiled. \
A setup screen appears at startup so you can enter your server address, room, \
name and team before playing. Use the NET button (bottom-left corner) at any \
time to disconnect or change connection settings. \
Requires the RecompExternalPython (REPY) extlib for networking. \

Default public server: anchor.hm64.org:43383"""

# A short description that will show up in this mod's entry in the mod list. This should be human readable and kept short
# to prevent it from being cut off due to the limited space.
short_description = "Anchor multiplayer, play with friends over the internet."

# Authors of this mod.
authors = ["Killzbro"]

# Anchor multiplayer for MNSG – uses RecompExternalPython (REPY) for TCP networking.

# ID of the target recomp game.
game_id = "mnsg"

# Minimum version of the target recomp (e.g. Zelda 64: Recompiled) that this mod can run on.
minimum_recomp_version = "0.1.0"

# Dependency mods. Each entry is the mod's ID and then an optional minimum version of the dependency mod.
dependencies = [
    # RecompExternalPython (REPY) v2.0.0 – provides the embedded Python 3.14 interpreter
    # used for TCP networking with the Anchor server.
    # https://github.com/LT-Schmiddy/zelda64recomp-python-extlibs-mod
    "RecompExternalPython_API:2.0.0",
]

# Native libraries (e.g. DLLs) and the functions they export.
native_libraries = [
    # Example native library:
    #    { name = "my_native_library", funcs = ["my_native_library_function"] }
]

# Inputs to the mod tool.
[inputs]

# Input elf file to generate a mod from.
elf_path = "build/mod.elf"

# Output mod filename.
mod_filename = "mnsg_multiplayer_anchor"

# Reference symbol files.
func_reference_syms_file = "Goemon64RecompSyms/mnsg.us.syms.toml"
data_reference_syms_files = ["Goemon64RecompSyms/mnsg.us.datasyms.toml"]

# Additional files to include in the mod.
# anchor_mnsg.py is the Python TCP client for the Anchor co-op server.
# It is embedded in the .nrm and importable thanks to REPY_PREINIT_ADD_NRM_TO_ALL_INTERPRETERS.
additional_files = ["py/anchor_mnsg.py"]

[[manifest.config_options]]
id = "anchor_host"
name = "Server Address (default)"
description = "Default hostname or IP pre-filled in the startup connection screen."
type = "String"
default = "localhost"

[[manifest.config_options]]
id = "anchor_port"
name = "Server Port (default)"
description = "Default TCP port pre-filled in the startup connection screen."
type = "Number"
min = 1
max = 65535
step = 1
precision = 0
percent = false
default = 43383

[[manifest.config_options]]
id = "anchor_room_id"
name = "Room ID (default)"
description = "Default room ID pre-filled in the startup connection screen."
type = "String"
default = "mnsg-recomp"

[[manifest.config_options]]
id = "anchor_player_name"
name = "Player Name (default)"
description = "Default player name pre-filled in the startup connection screen."
type = "String"
default = "Player"

[[manifest.config_options]]
id = "anchor_team_id"
name = "Team ID (default)"
description = "Default team ID pre-filled in the startup connection screen."
type = "String"
default = "default"

[[manifest.config_options]]
id = "anchor_show_room_hex"
name = "Show Room ID (Hex)"
description = "Display the raw hexadecimal room ID next to the area name in the player list (e.g. \"Oedo Castle 1F (0x00A5)\")."
type = "Enum"
options = ["Enabled", "Disabled"]
default = "Disabled"
